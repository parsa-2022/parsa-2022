# 🚀 راهنمای شروع سریع WORLD STRATEGY

## **راه ساده‌ترین:**

### **ویندوز 🪟**
1. فایل `setup.bat` را دوبل‌کلیک کنید
2. منتظر بمانید تا نصب تمام شود
3. بازی خودکار باز می‌شود!

### **Mac 🍎**
1. فایل `setup.sh` را دوبل‌کلیک کنید (یا از Terminal اجرا کنید)
2. منتظر بمانید
3. بازی باز می‌شود!

### **Linux 🐧**
```bash
chmod +x setup.sh
./setup.sh
```

---

## **اگر بالا نرفت:**

### **ویندوز:**
1. `Windows + R` بزنید
2. `cmd` بنویسید و Enter بزنید
3. این دستورات را بزنید:
```bash
cd WORLD-STRATEGY
npm install
npm run dev
```

### **Mac/Linux:**
1. Terminal باز کنید
2. این دستورات را بزنید:
```bash
cd WORLD-STRATEGY
npm install
npm run dev
```

---

## **کنترل‌های بازی:**

🖱️ **راست‌کلیک + کشیدن**: حرکت دوربین  
🖱️ **چرخ موس**: زوم در/خارج  

---

## **اگر مشکل داشتید:**

❌ **"Node.js نصب نشده"**
→ https://nodejs.org/ را نصب کنید

❌ **"Port already in use"**
→ Port متفاوتی استفاده کنید:
```bash
npm run dev -- --port 5174
```

---

**آماده برای بازی! 🎮**
